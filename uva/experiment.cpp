#include <bits/stdc++.h>

using namespace std;

main()
{
	/*cout << "This is an example of flush."<< endl << flush;
	cout << "Another line.";
	*/
    /*
	string name = "Sajid Hasan", na;
	na = name;

	//cin >> name;
	//getline(cin, name);
	for(string::size_type i = 0; i < name.length(); i++){
        name[i] = toupper(name[i]);
	}
    reverse(na.begin(), na.end());
	cout << na << endl;

    string line;
    gets(line);
    cout << line;
    */

    char name[1001];
    scanf("%[^\n]", name);
    string n = name;
    cout << n;

	return 0;
}

#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    ios::sync_with_stdio(false);cin.tie(0);
    ll n, q;
    cin >> n >> q;
    vector<
    for(ll i=0; i<n; i++)

    return 0;
}
